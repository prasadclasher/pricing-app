spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/pricing_feed}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:Freshdesk@1}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080
